# Hypridle Configuration
# Screen idle and lock management

general {
    lock_cmd = hyprlock                            # lock screen
    before_sleep_cmd = loginctl lock-session       # lock before suspend
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "on" })' # turn on display after wakeup
    inhibit_sleep = 3                              # wait until screen is locked
}

listener {
    timeout = 300                      # 5min
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 330                                            # 5.5min
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "off" })'                   # screen off when timeout has passed
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "on" })' && brightnessctl -r # screen on when activity is detected
}
